[package]
name = "staros-kernel-fuzz"
version = "0.0.0"
publish = false
edition = "2021"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
staros-kernel = { path = "..", features = ["std"] }

[[bin]]
name = "dtb_parser"
path = "fuzz_targets/dtb_parser.rs"
doc = false
